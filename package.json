{
  "name": "devlearn-hub",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "db": "node -e \"console.table(require('better-sqlite3')('data.db').prepare('SELECT id, email FROM users').all())\"",
    "logs": "node -e \"console.table(require('better-sqlite3')('data.db').prepare('SELECT id, email, success, attempted_at FROM login_attempts ORDER BY attempted_at DESC LIMIT 20').all())\""
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^12.9.0",
    "cookie-parser": "^1.4.7",
    "csurf": "^1.11.0",
    "ejs": "^3.1.9",
    "express": "4.18.2",
    "express-rate-limit": "^8.3.2",
    "express-session": "^1.19.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "nodemon": "3.0.1"
  }
}
